{
  "name": "@sentientops/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate:openapi": "openapi-typescript http://localhost:8000/openapi.json -o src/generated/openapi.ts",
    "lint": "echo \"No lint configured for contracts\"",
    "test": "echo \"No tests configured for contracts\""
  },
  "devDependencies": {
    "openapi-typescript": "^7.10.1",
    "typescript": "^5.8.3"
  }
}
